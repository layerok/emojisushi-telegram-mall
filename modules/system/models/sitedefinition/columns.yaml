# ===================================
#  Column Definitions
# ===================================

columns:
    name:
        label: Name
        searchable: true

    code:
        label: Code
        searchable: true

    theme:
        label: Theme
        type: selectable

    locale:
        label: Locale
        type: selectable

    timezone:
        label: Timezone
        type: selectable

    base_url:
        label: Base URL
        type: linkage
        sortable: false
        attributes:
            target: _blank

    status_code:
        label: Status
        type: selectable
        sortable: false

    active_color:
        label: Color
        shortLabel: ''
        type: colorpicker
        sortable: false
        cssClass: text-end
